<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright (C) 2025 VPA Software Limited -->
<!-- License OPL-1 - See LICENSE file for full copyright and licensing details. -->
<odoo>
    <!--
    IMPORTANT: This module uses standard Odoo accounting groups for security:
    - account.group_account_user: Can create and view transfers
    - account.group_account_manager: Can approve, reject, cancel transfers and configure settings

    No custom security groups are used to ensure the module works immediately
    for any user who already has accounting access.
    -->

    <!-- Record Rules using standard Odoo accounting groups -->

    <!-- All accounting users can read all transfers in their company -->
    <record id="internal_transfer_rule_read_all" model="ir.rule">
        <field name="name">Internal Transfer: Read All Company Transfers</field>
        <field name="model_id" ref="model_internal_transfer"/>
        <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('account.group_account_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Accounting users can write/create their own draft transfers -->
    <record id="internal_transfer_rule_user_write" model="ir.rule">
        <field name="name">Internal Transfer: User Write Own Drafts</field>
        <field name="model_id" ref="model_internal_transfer"/>
        <field name="domain_force">[
            ('company_id', 'in', company_ids),
            ('create_uid', '=', user.id),
            ('state', '=', 'draft')
        ]</field>
        <field name="groups" eval="[(4, ref('account.group_account_user'))]"/>
        <field name="perm_read" eval="False"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Account Managers have full write access to all transfers -->
    <record id="internal_transfer_rule_manager_write" model="ir.rule">
        <field name="name">Internal Transfer: Manager Full Access</field>
        <field name="model_id" ref="model_internal_transfer"/>
        <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('account.group_account_manager'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Threshold Rules - Company-based for all accounting users -->
    <record id="internal_transfer_threshold_rule" model="ir.rule">
        <field name="name">Threshold: Company Rule</field>
        <field name="model_id" ref="model_internal_transfer_threshold"/>
        <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('account.group_account_user'))]"/>
    </record>
</odoo>
